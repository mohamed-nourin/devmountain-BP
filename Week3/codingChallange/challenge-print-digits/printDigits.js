function printDigits(num) {
    while (num > 0) {
      const digit = num % 10;
      console.log(digit);
      num = Math.floor(num / 10);
    }
  }
  

function printDigits (num) {
    while(num>0)
}